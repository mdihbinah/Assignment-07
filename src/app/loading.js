import React from 'react';

const Loading = () => {
    return (
        <div className='flex justify-around items-center h-[60vh]'>
            <span className="loading loading-bars loading-xl text-error "></span>
        </div>
    );
};

export default Loading;